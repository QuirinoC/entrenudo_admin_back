const axios = require('axios');
const URL = process.env.url | 'localhost:8080/api'


test("Get orders for DB", () => {
  return Promise.resolve(() => {});
});

test("Store post to DB", () => {
  return Promise.resolve(() => {});
});

test("Store image to AWS", () => {
  return Promise.resolve(() => {});
});

test("Connect to DB", () => {
  return Promise.resolve(() => {});
});